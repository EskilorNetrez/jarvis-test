#include "system/ScreenContext.h"

namespace jarvis {

std::string ScreenContext::CaptureAndSummarize() {
    // Integration hook: Desktop Duplication API + local OCR (Tesseract / WinRT OCR).
    return "Screen context unavailable in starter mode; UI and automation channels are live.";
}

} // namespace jarvis
